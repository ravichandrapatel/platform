# Terraform (sample workflow)

This Terraform pipeline is provided as a **reference implementation**.

- It is **not intended to be released** as a reusable workflow.
- Keep it under `workflows/terraform/` as the source-of-truth documentation set.

If you want to use it in another repo, copy/adapt it into that repo’s `.github/workflows/`.
