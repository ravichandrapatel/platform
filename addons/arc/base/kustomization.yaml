# Base: shared namespace for ARC (controller + scale set).
# Used by both OpenShift and Kubernetes overlays.

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
