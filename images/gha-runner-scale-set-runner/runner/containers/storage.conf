# VFS driver so rootless Podman works without /dev/fuse (OpenShift/ARC pods).
# ignore_chown_errors helps when running as non-root (UID 1001) and chown fails.
[storage]
driver = "vfs"
[storage.options.vfs]
ignore_chown_errors = "true"
